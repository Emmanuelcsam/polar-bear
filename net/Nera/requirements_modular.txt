# Neural Nexus IDE Server v6.0 Enhanced Requirements
# ===================================================

# Core Requirements (Essential)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0
aiofiles>=23.0
psutil>=5.9.0

# Performance Enhancements (Highly Recommended)
uvloop>=0.17.0          # Enhanced event loop (4x faster)
orjson>=3.9.0           # Ultra-fast JSON (6x faster)

# Enhanced Logging
loguru>=0.7.0           # Structured logging with colors

# Security & Rate Limiting
slowapi>=0.1.9          # Rate limiting middleware

# Code Analysis Tools (Open Source)
mypy>=1.5.0             # Type checking
pylint>=2.17.0          # Advanced static analysis
ruff>=0.1.0             # Fast linting and formatting
bandit>=1.7.5           # Security vulnerability scanner

# Security Analysis (Optional but Recommended)
# Note: semgrep installation may require additional system dependencies
# semgrep>=1.45.0       # Advanced security scanning

# Enhanced Error Handling
friendly-traceback>=1.7.0  # Better error messages

# Fast Serialization (Optional)
msgspec>=0.18.0         # High-performance serialization

# AI Integration (Optional)
openai>=1.0.0           # OpenAI API integration
requests>=2.31.0        # HTTP requests for API calls

# Development & Testing (Optional)
pytest>=7.4.0          # Testing framework
black>=23.0.0           # Code formatting (alternative to ruff)
isort>=5.12.0           # Import sorting

# Additional Utilities
python-multipart>=0.0.6    # Form data parsing
python-jose[cryptography]>=3.3.0  # JWT handling
passlib[bcrypt]>=1.7.4     # Password hashing
